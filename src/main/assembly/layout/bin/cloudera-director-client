#!/bin/sh

if [ $# -le 0 ]; then
	VERSION="5"
else
	VERSION="$1"
fi

yum remove -y "bigtop*" "hadoop*" "zookeeper*" "hbase*" "hive*" "impala*"

sleep 5

yum install -y --enablerepo=cloudera-cdh$VERSION hadoop-client hive impala-shell hive-hbase hadoop-hdfs-fuse solr-mapreduce
