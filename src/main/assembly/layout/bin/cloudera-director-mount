#!/bin/sh

source "$LAUNCHPAD_WRAPPER_HOME"/bin/cloudera-director.env

mkdir -fp $CLUSTER_FUSE_MOUNT > /dev/null 2>&1

umount $CLUSTER_FUSE_MOUNT > /dev/null 2>&1
hadoop-fuse-dfs $CLUSTER_NAMENODE $CLUSTER_FUSE_MOUNT > /dev/null

